<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yungura.profashional">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Hompage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserLoginActivity"></activity>
        <activity android:name=".UserDisplay"></activity>
        <activity android:name=".UserSignUp"></activity>
        <activity android:name=".StylistLoginActivity"></activity>
        <activity android:name=".StylistDisplay"></activity>
        <activity android:name=".StylistSignUp"></activity>
    </application>

</manifest>